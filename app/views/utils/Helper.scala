package views.utils

object Helper {
	val Configuration = globals.Configuration
	val Applicaton = globals.Application
	type Flash = play.api.mvc.Flash
	import context.ViewContext
	//	type ViewContext = context.ViewContext.type
	//type Form[A] = play.api.data.Form[A]
}